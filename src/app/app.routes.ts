import { Routes } from '@angular/router';
import { RequestFormComponent } from './request-form/request-form.component'; // Import the Request Form component

export const routes: Routes = [
  { path: 'request-form', component: RequestFormComponent }, // Add route for the form
];
